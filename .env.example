# Optional. A personal access token raises the GitHub API rate limit # from 60 requests/hour to 5000 requests/hour. No scopes are needed # for public repos. # Create one at: https://github.com/settings/tokens GITHUB_TOKEN=
